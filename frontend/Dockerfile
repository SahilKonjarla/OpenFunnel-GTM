# frontend/Dockerfile

# Temporary placeholder Dockerfile for frontend
FROM alpine:3.18

# Dummy command to keep the container alive
CMD ["sh", "-c", "echo Frontend placeholder is running... && sleep infinity"]
